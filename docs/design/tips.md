1. 对于小功能来说，可以我写前端，kira review 就行
2. 先不考虑流量，实时性 -> 项目逻辑： 接口 + 存储(缓存层级的存储：速度的诉求，什么阶段到mysql，什么阶段到redis)； 什么数据适合放在redis中
3. 架构： 单体； 高热流量的场景，所有的网络请求置信度低，减少任何非必要的网络请求，尽可能少的引入中间件 -- 微服务，请求次数少，又非常重要，但是必须成功 （支付的时候跟其他服务去通信，不如做两个单体服务）
4. 监控： 出现问题能不能及时发现（打日志，打metrix，报警配置，数据看板 ）
5. 容灾（自动恢复，保证的是其中一个中间件没了怎么办，而不是全部中间件没了怎么办）： 出现问题有没有预案 -- 比如说 mysql 挂了怎么办（可以从流程上避免），redis 挂了怎么办（abase避免）， 服务挂掉了（内存中存一份，redis中存一份）
6. AI 的流程： 设计 -> 开发 -> 测试 -> 修复 -> 上线   (测试和修复是循环的); 开发的话有需求调研，产品调研； 让 AI 去闭环；让AI连apifox，curl去测，告诉ai怎么去拿数据，让ai去做验证，然后让ai上线， 让ai全流程跑通，这里设计测试文档需要考虑很多东西
7. redis 的读写分离。
8. 最后可以上个 吴哥 的集群=