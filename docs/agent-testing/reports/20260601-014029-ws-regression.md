# 测试报告：WebSocket heartbeat and online presence

## 基本信息

- 测试目标：验证服务端主动心跳和 WebSocket 在线数可信度回归点。
- 测试类型：本地单元测试 / 回归测试。
- 测试时间：2026-06-01 01:40:29 Asia/Shanghai。
- 执行 agent：Codex 主 agent。
- 主 agent：Codex。
- 子 agent：未使用。
- 子 agent 结果摘要：未使用。
- 主 agent 复核结论：未使用。
- 冲突和处理：未使用 subagent；测试前发现 `modules/ws.md` 残留旧 `HINCRBY` 在线数口径，已按当前契约修正为 `online_count = SCARD online_users` 后执行。
- Subagent cleanup：未使用。
- 并行数据隔离证明：不适用。
- 读取文档：`docs/agent-testing/README.md`、`docs/agent-testing/templates/protocol.md`、`docs/agent-testing/guides/runner.md`、`docs/agent-testing/modules/ws.md`、`docs/agent-testing/reports/README.md`。

## 测试环境

- 服务地址：未启动服务。
- 配置来源：本地 Go 单元测试。
- MySQL：未使用。
- Redis：未连接真实 Redis，使用 fake presence store。
- Apifox：未执行接口契约测试。
- WebSocket：未打开真实端口，使用 fake socket 覆盖 control ping / pong deadline 行为。

## 依赖策略

| 依赖 | 使用方式 | 原因 |
| --- | --- | --- |
| MySQL | 未使用 | 本次只验证 Hub / Conn 本地行为 |
| Redis | fake presence store | 本地单元测试不得连接真实 Redis |
| WebSocket | fake socket | 避免真实端口和外部连接，稳定验证 control ping / pong |
| 外部服务 | 未使用 | 本次无第三方依赖 |

## 测试数据

- 测试批次 ID：agent_ws_20260601_014029。
- 创建数据：无持久化数据；仅创建进程内 fake Conn、fake socket、fake presence store。
- 复用数据：无。

## 执行步骤

1. 按 README 路由读取通用协议、runner 指南和 WebSocket 模块契约。
2. 修正 WebSocket 模块契约中残留的旧在线数口径。
3. 精确执行 heartbeat / online presence 相关回归用例。
4. 执行 WebSocket 包测试。
5. 执行全量 Go 测试。

## 验证证据

| 验证点 | 证据 | 结果 |
| --- | --- | --- |
| 同用户同房间重复连接替换旧连接 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/ws/hub -run 'Test(StartWriteLoopSendsServerControlPing|StartReadLoopConfiguresPongDeadlineRefresh|CloseConnRemovesPresenceOnce|ClosingReplacedConnectionDoesNotRemoveNewPresence|RegisterSameUserRoomReplacesOldConnection)' -v` 中 `TestRegisterSameUserRoomReplacesOldConnection` PASS | 通过 |
| 关闭路径幂等，不重复触发在线状态移除 | 同一命令中 `TestCloseConnRemovesPresenceOnce` PASS | 通过 |
| 被替换旧连接关闭不会删除新连接在线状态 | 同一命令中 `TestClosingReplacedConnectionDoesNotRemoveNewPresence` PASS | 通过 |
| 服务端主动 control ping | 同一命令中 `TestStartWriteLoopSendsServerControlPing` PASS | 通过 |
| control pong 刷新 read deadline | 同一命令中 `TestStartReadLoopConfiguresPongDeadlineRefresh` PASS | 通过 |
| WebSocket 模块回归 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/ws/...`，退出码 0；`handler`、`hub` 均 ok | 通过 |
| 全量回归 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./...`，退出码 0 | 通过 |

## 通过项

- 服务端主动心跳已有本地回归覆盖：write loop 定时发送 WebSocket control `ping`。
- read loop 已配置 control `pong` handler，并能刷新 read deadline。
- Hub 关闭路径幂等，重复 close 不会重复执行有效 presence leave。
- 同一用户同房间重复连接时，新连接替换旧连接，旧连接后续关闭不会误删新连接在线状态。
- WebSocket 模块测试和全量 Go 测试均通过。

## 失败项

- 无。

## 跳过项

- 真实 Redis 集成验证：本次按本地单元测试策略执行，未连接真实 Redis。风险是未直接验证 `SADD/SREM/SCARD/HSET` 与真实 Redis 的交互。后续补测条件：按 `guides/environment.md` 准备测试 Redis，并获得真实依赖执行批准。
- 真实 WebSocket 握手 / 异常断网测试：本次未启动服务、未打开端口。风险是未覆盖 gorilla websocket 与网络异常的端到端行为。后续补测条件：启动本地服务，使用测试 token 和测试 Redis 执行模块集成测试。
- Apifox 对齐：本次不是接口契约测试，未执行。

## Apifox 对齐偏差

- 不适用。本次未执行接口契约测试。

## 风险和建议

- 建议后续补一轮真实 Redis 模块集成测试，验证 `online_count` 在 Redis 中确实等于 `SCARD online_users`，并覆盖漂移后下一次 join / leave 自动收敛。
- 建议补真实 WebSocket 小规模场景：客户端仅响应 control ping、不发送文本 ping 时连接保持；客户端断开后 Hub 和 Redis 在线状态收敛。

## 建议沉淀的回归测试

- 保留当前 `internal/app/ws/hub/hub_test.go` 中 heartbeat、关闭幂等和同用户同房间替换连接用例。
- 后续新增一个真实 Redis runner 或集成测试，专门验证 `redisPresenceStore.JoinRoom` / `LeaveRoom` 的 `SCARD` 回写行为。

## 已知缺口

- 未覆盖真实 Redis。
- 未覆盖真实 WebSocket 网络层。
- 未覆盖接口契约和 Apifox 对齐。

## 测试数据清理结果

- 测试批次 ID：agent_ws_20260601_014029。
- 创建的数据：无持久化数据。
- 清理方式：Go 测试进程退出后释放进程内 fake 数据。
- 清理结果：无需清理 Redis、MySQL 或服务进程。
- 未清理原因：不适用。
