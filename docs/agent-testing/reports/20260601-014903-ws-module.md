# 测试报告：WebSocket module

## 基本信息

- 测试目标：按 `docs/agent-testing/modules/ws.md` 验证当前可本地执行的 WebSocket 模块契约。
- 测试类型：本地单元测试 / 回归测试。
- 测试时间：2026-06-01 01:49:03 Asia/Shanghai。
- 执行 agent：Codex 主 agent。
- 主 agent：Codex。
- 子 agent：未使用。
- 子 agent 结果摘要：未使用。
- 主 agent 复核结论：未使用。
- 冲突和处理：无 subagent 冲突；本轮按当前 `ws.md` 的本地单元测试边界执行，真实 Redis / HTTP / WebSocket 集成场景列为跳过项。
- Subagent cleanup：未使用。
- 并行数据隔离证明：不适用。
- 读取文档：`docs/agent-testing/README.md`、`docs/agent-testing/templates/protocol.md`、`docs/agent-testing/guides/runner.md`、`docs/agent-testing/modules/ws.md`、`docs/agent-testing/reports/README.md`。

## 测试环境

- 服务地址：未启动服务。
- 配置来源：本地 Go 单元测试。
- MySQL：未使用。
- Redis：未连接真实 Redis；Hub presence 使用 fake store。
- Apifox：未执行接口契约测试。
- WebSocket：未打开真实端口；Conn 使用 fake socket 覆盖 control ping / pong 行为。

## 依赖策略

| 依赖 | 使用方式 | 原因 |
| --- | --- | --- |
| MySQL | 未使用 | 本轮只覆盖 WebSocket 本地单元契约 |
| Redis | fake presence store | 本地单元测试禁止直连 Redis |
| WebSocket | fake socket / in-process Conn | 避免端口和外部连接，稳定验证连接生命周期 |
| 外部服务 | 未使用 | 本模块本地回归无需第三方依赖 |

## 测试数据

- 测试批次 ID：agent_ws_20260601_014903。
- 创建数据：无持久化数据；仅创建进程内 Hub、Conn、fake socket、fake presence store。
- 复用数据：无。

## 执行步骤

1. 读取 `ws.md` 的模块目标、边界、依赖策略、Hub / Conn 测试契约、状态一致性和通过标准。
2. 执行 Hub 单元测试，覆盖 Register / Remove、Fanout、Unicast、慢连接、关闭幂等、服务端 heartbeat 和 snapshot delivery。
3. 执行 handler origin 单元测试。
4. 执行 `pkg/wsevent` topic / addr / JSON 测试。
5. 执行 item service 中与 WebSocket `auction_snapshot` / `time_sync` 相关的回归测试。
6. 执行 WebSocket 包整体测试。
7. 执行全量 Go 回归测试。

## 验证证据

| 验证点 | 证据 | 结果 |
| --- | --- | --- |
| Hub Register / Remove 索引维护 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/ws/hub -v` 中 `TestRegisterAddsToIndexes`、`TestRemoveCleansIndexes` PASS | 通过 |
| 同用户同房间重复连接替换旧连接 | 同一命令中 `TestRegisterSameUserRoomReplacesOldConnection` PASS | 通过 |
| 关闭路径幂等和在线状态不重复扣减 | 同一命令中 `TestCloseConnRemovesPresenceOnce`、`TestCloseConnIsIdempotentAndClosedConnDeliveryDoesNotPanic` PASS | 通过 |
| 旧连接被替换后关闭不误删新连接在线状态 | 同一命令中 `TestClosingReplacedConnectionDoesNotRemoveNewPresence` PASS | 通过 |
| 房间广播隔离 | 同一命令中 `TestFanoutDeliversToRoom`、`TestSendToRoomDeliversEvent` PASS | 通过 |
| 用户单播 | 同一命令中 `TestUnicastDeliversToUser` PASS | 通过 |
| 慢连接剔除 | 同一命令中 `TestSlowConnectionIsClosedOnFullChannel` PASS | 通过 |
| 并发发送和关闭不 panic | 同一命令中 `TestConcurrentSendAndCloseDoesNotPanic` PASS | 通过 |
| 服务端主动 heartbeat | 同一命令中 `TestStartWriteLoopSendsServerControlPing` PASS | 通过 |
| control pong 刷新 read deadline | 同一命令中 `TestStartReadLoopConfiguresPongDeadlineRefresh` PASS | 通过 |
| 连接后当前拍品快照 | 同一命令中 `TestRegisterDeliversSnapshotWhenProviderHasOne` PASS | 通过 |
| Origin checker | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/ws/handler -v` 中 3 个 `TestConfigureOriginChecker...` 用例 PASS | 通过 |
| topic / addr / event JSON | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./pkg/wsevent -v` 中 `TestRoomTopic`、`TestUserAddr`、`TestEventJSON` PASS | 通过 |
| `auction_snapshot` / `time_sync` | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/item/service -run 'TestBroadcastTimeSync|TestAuctionSnapshot' -v` 中 5 个用例 PASS | 通过 |
| WebSocket 包整体 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/ws/...` 退出码 0 | 通过 |
| 全量回归 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./...` 退出码 0 | 通过 |

## 通过项

- Hub 内存索引、房间广播、用户单播、慢连接剔除、关闭幂等和并发发送关闭均有本地回归覆盖。
- 服务端主动 WebSocket control `ping` 和 control `pong` 刷新 deadline 有本地回归覆盖。
- 同用户同房间重复连接替换旧连接有本地回归覆盖。
- 连接注册后的 `auction_snapshot` delivery 和每秒 `time_sync` 的核心生成逻辑有本地回归覆盖。
- `pkg/wsevent` topic / addr 格式和事件 JSON 序列化有回归覆盖。
- WebSocket 包测试和全量 Go 测试均通过。

## 失败项

- 无。

## 跳过项

- `POST /api/v1/ws-ticket` 接口契约：本轮未启动真实服务、未使用测试 token 和测试 Redis。风险是未直接验证 ticket TTL、响应结构、GETDEL 一次性消费。后续补测条件：按 `guides/environment.md` 准备测试服务和 Redis。
- `GET /ws/v1/rooms/{room_id}` 真实握手：本轮未打开真实 WebSocket 端口。风险是未覆盖 HTTP 400/401/101、真实 upgrade 和 ticket query。后续补测条件：启动本地服务并使用本批次测试 ticket。
- Redis online state 集成：本轮未连接真实 Redis。风险是未直接验证 `SADD/SREM/SCARD/HSET` 在 Redis 中的最终值。后续补测条件：使用测试 Redis，并限定本批次 room key。
- 真实业务事件端到端：本轮未通过 HTTP 创建房间、拍品和出价。风险是未验证真实客户端消息序列与 HTTP / Redis / MySQL 最终状态一致。后续补测条件：按 room / item / bid 契约准备测试数据。
- 并发握手 / Redis 在线状态并发增减：本轮未执行真实并发依赖测试。后续补测条件：输出并发场景设计并获得执行批准。
- 日志敏感信息检查：本轮未启动服务、未采集服务日志。后续补测条件：接口契约测试时同时采集日志摘要。
- Apifox 对齐：本轮不是接口契约测试，未执行。

## Apifox 对齐偏差

- 不适用。本轮未执行接口契约测试。

## 风险和建议

- 建议下一轮执行真实 Redis + WebSocket 小规模模块集成测试，重点补齐 ticket、GETDEL、online state 和真实断线收敛证据。
- 建议后续为 `redisPresenceStore.JoinRoom` / `LeaveRoom` 增加真实 Redis 集成 runner，验证漂移后下一次 join / leave 自动按 `SCARD` 收敛。
- 建议真实场景测试覆盖重连恢复：断开后通过房间详情、商品详情、排行榜恢复现场，再重连接收 `auction_snapshot` / `time_sync`。

## 建议沉淀的回归测试

- 保留当前 Hub / Conn / wsevent / time_sync 单元测试作为本地快速回归。
- 新增真实 Redis runner：只操作 `agent_ws_` 批次 room key，验证 online users 和 online count。
- 新增真实 WebSocket runner：验证 ticket 一次性消费、ping / leave_room、异常断开和重连恢复。

## 已知缺口

- 未覆盖真实 Redis。
- 未覆盖真实 WebSocket upgrade。
- 未覆盖真实 HTTP ticket 接口。
- 未覆盖真实业务事件端到端。
- 未覆盖并发一致性和性能压测。

## 测试数据清理结果

- 测试批次 ID：agent_ws_20260601_014903。
- 创建的数据：无持久化数据。
- 清理方式：Go 测试进程退出后释放进程内 fake 数据。
- 清理结果：无需清理 Redis、MySQL 或服务进程。
- 未清理原因：不适用。
