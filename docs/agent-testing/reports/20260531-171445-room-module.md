# 测试报告：room module

## 基本信息

- 测试目标：按 `docs/agent-testing/modules/room.md` 验证房间模块接口、状态流转、Redis 副作用、`current_item_id` 响应与跨 item/room 状态一致性。
- 测试类型：本地单元测试、接口契约测试、模块集成测试、场景测试、状态一致性测试。
- 测试时间：2026-05-31 17:00-17:14 Asia/Shanghai。
- 执行 agent：主 agent + 2 个 subagent。
- 主 agent：读取测试入口、启动/停止本地服务、复核 subagent 结果、沉淀报告。
- 子 agent：
  - `agent_room_lifecycle_20260531_1702`：room 基础生命周期，场景 1-5。
  - `agent_room_current_20260531_1702`：item queue 与 `current_item_id` 跨模块一致性，场景 6-7。
- 子 agent 结果摘要：lifecycle runner `PASS: 7 FAIL: 0`；current runner `PASS: 8 FAIL: 0`。
- 主 agent 复核结论：两组子任务数据批次隔离，结论互补无冲突；业务测试通过。主 agent 复核了 lifecycle `evidence.md`、current 子任务完成摘要、OpenAPI room 路径字段和本地单元测试输出。
- 冲突和处理：无业务冲突。Apifox/OAS 与实际错误响应示例、`current_item_id` required 语义存在对齐偏差，已单独记录。
- Subagent cleanup：两个 subagent 均已关闭；本地后端服务已停止，8080 未监听。
- 并行数据隔离证明：
  - lifecycle 子任务使用 batch id `agent_room_lifecycle_20260531_1702`，账号和房间 title 均带此前缀。
  - current 子任务使用 batch id `agent_room_current_20260531_1702`，账号、房间 title、商品 title 均带此前缀。
  - 两个子任务只清理各自批次 DB 行和各自 room/item Redis key。
- 读取文档：`docs/agent-testing/README.md`、`templates/protocol.md`、`guides/runner.md`、`guides/environment.md`、`guides/go-runner.md`、`guides/subagent.md`、`reports/README.md`、`modules/room.md`，current 子任务按需读取 `modules/item.md` 相关小节。

## 测试环境

- 服务地址：本地后端 `http://127.0.0.1:8080`，测试后已停止。
- 配置来源：`config.yaml`。
- MySQL：本地 Docker MySQL，凭据未写入报告。
- Redis：本地 Docker Redis，凭据未写入报告。
- Apifox：通过项目 OAS MCP 读取当前规范。
- WebSocket：不适用，room 模块无开播/下播 WebSocket 广播。

## 依赖策略

| 依赖 | 使用方式 | 原因 |
| --- | --- | --- |
| MySQL | 本地真实依赖 | 验证 `live_rooms`、`users`、`auction_items`、`auction_rules` 实际状态 |
| Redis | 本地真实依赖 | 验证 room state、item queue、item state |
| HTTP 服务 | 本地真实后端 | 验证真实路由、鉴权、响应字段 |
| WebSocket | 未使用 | room 模块无对应副作用 |
| 外部服务 | 未使用 | 不属于 room 模块 |

## 测试数据

- 测试批次 ID：
  - `agent_room_lifecycle_20260531_1702`
  - `agent_room_current_20260531_1702`
- 创建数据：
  - lifecycle：3 个测试账号、2 个直播间。
  - current：1 个商家用户、1 个直播间、2 个商品、2 条规则。
- 复用数据：本地 MySQL/Redis 服务和本地后端进程；未复用业务数据。

## 执行步骤

1. 启动/确认本地 MySQL 与 Redis healthy。
2. 运行本地单元测试：`go test ./internal/app/room/...`、`go test ./internal/app/item/service`、`go test ./...`。
3. 主 agent 启动本地后端服务。
4. 并行执行两个 subagent runner，分别使用独立 batch id。
5. 读取 OAS room 路径，检查 room 接口字段。
6. 停止本地后端服务并关闭 subagent。
7. 复核证据并写入本报告。

## 验证证据

| 验证点 | 证据 | 结果 |
| --- | --- | --- |
| 本地单元测试 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/room/...`；room dto/service 通过 | PASS |
| item service 回归 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./internal/app/item/service` | PASS |
| 全仓编译和单测 | `rtk env GOCACHE=/tmp/live-auction-go-cache go test ./...` | PASS |
| 场景 1 激活并开播 | lifecycle evidence：`POST /merchant/room` 200，`POST /rooms/{id}/start` 200，详情/商家视图 `status=live`，DB `status=live`，Redis `status=live` | PASS |
| 场景 2 下播 | lifecycle evidence：`POST /rooms/{id}/end` 200，商家视图 `status=idle`，DB `status=idle,current_item_id=""`，Redis `status=idle,current_item_id=""` | PASS |
| 场景 3 激活幂等 | lifecycle evidence：二次 `POST /merchant/room` 返回同一 room ID，DB 该 merchant 仅 1 条 room，title 未覆盖 | PASS |
| 场景 4 归属隔离 | lifecycle evidence：merchant B 操作 merchant A start/end 均 404，实际 `code=40401,message=not found`，DB 状态不变 | PASS |
| 场景 5 公开列表只展示 live | lifecycle evidence：room A live、room B idle，`GET /api/v1/rooms` 只返回 room A | PASS |
| 关键响应字段 | lifecycle evidence：商家视图包含 `current_item_id`、`online_count`、`queued_count`、`actions`、`status_text`；公开列表/详情包含 `current_item_id`，详情 `item_queue` 为数组 | PASS |
| 场景 6 商品队列富化 | current summary：publish 两个商品 200；Redis room item_queue 与 `GET /rooms/{id}` `item_queue` 同序 | PASS |
| 场景 7 未开始商品时字段存在 | current summary：列表、详情、商家视图均 200 且 `current_item_id=""`；DB/Redis 为空 | PASS |
| 场景 7 start item 写当前商品 | current summary：`POST /items/{item_id}/start` 200；三类房间响应、DB、Redis 均为该 item ID | PASS |
| 场景 7 cancel item 清空 | current summary：`POST /items/{item_id}/cancel` 200；三类房间响应、DB、Redis 均清空；item state 已清理，room queue 仅剩第二个 item | PASS |
| 场景 7 room end 清空 | current summary：`POST /rooms/{id}/end` 200；DB `status=idle,current_item_id=""`；Redis `status=idle,current_item_id=""` | PASS |
| Apifox/OAS 字段存在 | OAS `/api/v1/rooms`、`/api/v1/rooms/{room_id}`、`/api/v1/merchant/room` 均包含 `current_item_id` property | PASS |

## 通过项

- `room.md` 场景 1-7 全部通过。
- `current_item_id` 空值响应、start item 写入、cancel item 清空、room end 清空均通过。
- `item_queue` Redis ZSET 与 room detail 响应一致。
- 鉴权、归属隔离、幂等、默认 live 列表过滤通过。
- 测试数据清理完成。

## 失败项

- 无。

## 跳过项

- 并发测试：本次用户批准真实依赖模块测试和 subagent 并行执行，但未批准 `guides/concurrency.md` 所需的专项并发计划；后续需单独设计并确认。
- WebSocket：room 模块文档标记不适用。
- 性能压测：不属于本次 room 模块验证范围。

## Apifox 对齐偏差

- OAS 中 room 成功响应 `data` 内字段只作为 `properties` 出现，未将 `current_item_id` 标为 required；`room.md` 和代码当前要求空值也必须返回该字段。
- OAS 中部分 404 错误响应示例仍为 `code=40001,message=invalid request`；真实归属隔离返回 `code=40401,message=not found`。

## 风险和建议

- 建议同步 Apifox/OAS required 字段和 404 示例，避免前端或测试工具误判。
- 并发状态动作仍未测，且文档中注明并发重复 start/end 的严格语义需确认。
- Redis 写失败仍为软失败且当前无可见告警，建议后续确认是否需要日志/指标。

## 建议沉淀的回归测试

- 保留 `RoomDetailDTO` / `MerchantRoomDTO` 空 `current_item_id` 序列化测试。
- 保留 item start/cancel 更新 room current item 的 service 单测。
- 增加接口级回归：`GET /api/v1/rooms`、`GET /api/v1/rooms/{room_id}`、`GET /api/v1/merchant/room` 空值时必须包含 `current_item_id:""`。
- 单独设计 room 并发一致性测试：并发激活、并发 start、并发 end。

## 已知缺口

- 未执行并发一致性测试。
- 未执行性能压测。
- 未验证线上或线上等价环境；本报告仅覆盖本地真实依赖。

## 测试数据清理结果

- `agent_room_lifecycle_20260531_1702`：
  - Redis room state/item_queue key 已删除。
  - `live_rooms.title LIKE batch%` 软删 2 行。
  - `users.account LIKE batch%` 软删 3 行。
  - 未清理原因：无。
- `agent_room_current_20260531_1702`：
  - 本批次 room state、item queue、item state 等 Redis 数据已清理。
  - 本批次用户、房间、商品、规则已清理或软删除。
  - 未清理原因：无。
