# 测试报告：bid

## 基本信息

- 测试目标：出价模块 hot path 集成验证，覆盖 Redis hot state、Redis Stream bid-log handoff、BidLog worker 异步落库、幂等和低价拒绝无副作用。
- 测试类型：模块集成测试 / 状态一致性测试。
- 测试时间：2026-06-04 18:39-18:54 Asia/Shanghai。
- 执行 agent：主 agent Codex。
- 主 agent：Codex。
- 子 agent：未使用。
- 子 agent 结果摘要：未使用。
- 主 agent 复核结论：未使用。
- 冲突和处理：无。
- Subagent cleanup：未使用。
- 并行数据隔离证明：不适用。
- 读取文档：`docs/agent-testing/README.md`、`docs/agent-testing/templates/protocol.md`、`docs/agent-testing/guides/runner.md`、`docs/agent-testing/modules/bid.md`、`docs/agent-testing/guides/environment.md`、`docs/agent-testing/guides/go-runner.md`、`docs/agent-testing/reports/README.md`。

## 测试环境

- 服务地址：本地服务 `127.0.0.1:8080`。
- 配置来源：本地 `config.yaml`，由 `config.yaml.example` 复制，未纳入 git。
- MySQL：本地 MySQL，具体 DSN 已省略。
- Redis：本地 Redis，无密码。
- Apifox：未执行接口规范对齐，本次不是接口契约专项测试。
- WebSocket：未连接真实 WebSocket。

## 依赖策略

| 依赖 | 使用方式 | 原因 |
| --- | --- | --- |
| MySQL | 本地真实 MySQL | 验证 `auction_items`、`auction_rules`、`bid_logs` 的真实持久化和最终一致 |
| Redis | 本地真实 Redis | 验证 Lua 原子更新、ranking、idempotency key 和 `auction:bid_log:stream` |
| HTTP 服务 | 本地后端进程 | 验证真实 handler/service/cache/DAO 链路 |
| WebSocket | 未使用 | 本次目标为 bid hot path 持久化链路，完整 WS 契约转 `modules/ws.md` |
| 外部服务 | 未使用 | 出价模块本场景不依赖第三方服务 |

## 测试数据

- 测试批次 ID：`agent_bid_hotpath_20260604183926`
- 创建数据：
  - 用户：`agent_bid_hotpath_20260604183926_merchant`、`agent_bid_hotpath_20260604183926_user1`、`agent_bid_hotpath_20260604183926_user2`
  - 拍品：`item_2062488165335175168`
  - 房间 ID：`room_agent_bid_hotpath_20260604183926`
  - 出价：`bid_2062488165444227072`、`bid_2062488166807375872`
- 复用数据：本地 MySQL/Redis 服务进程；未复用业务数据。

## 执行步骤

1. 启动本地 MySQL/Redis：`rtk docker compose up -d mysql redis`，确认现有容器 healthy。
2. 启动本地后端：`rtk env GOCACHE=/tmp/live-auction-go-cache go run main.go server -c config.yaml`。
3. 生成临时 Go runner：`/tmp/agent-runner-agent_bid_hotpath_20260604183926/main.go`。
4. 运行 runner：`rtk env GOCACHE=/tmp/live-auction-go-cache TEST_DSN=<omitted> go run main.go`。
5. runner 输出 5 个 CASE，SUMMARY 为 `PASS: 5  FAIL: 0`。
6. runner 执行 cleanup，删除本批次 `bid_logs` / `auction_rules`、软删除本批次 `auction_items` / `users`，删除本批次 item Redis keys。

## 验证证据

| 验证点 | 证据 | 结果 |
| --- | --- | --- |
| 账号、拍品和 Redis hot state 初始化 | CASE `prepare actors and ongoing item`：HTTP publish/start 均 `200`；DB `auction_items` 为 `ongoing`；Redis state 包含 `room_id=room_agent_bid_hotpath_20260604183926`、`bid_increment=100`、`status=ongoing` | PASS |
| 成功出价写入 Redis state/ranking/stream 并由 worker 落库 | CASE `successful bid appends stream and worker persists`：HTTP `200`，`bid_id=bid_2062488165444227072`；Redis state `current_price=1100`、`leader_user_id=user_2062488164429205504`；stream 事件 `1780570472489-0`；MySQL `bid_logs` 1 条 | PASS |
| 幂等重试不重复追加 stream 或 BidLog | CASE `idempotent retry does not duplicate stream or bid log`：同 key 重试 HTTP `200` 返回原 `bid_id`；stream count `1/1`；BidLog count `1`；Redis `bid_count=1`；TTL `24h0m0s` | PASS |
| 低价拒绝无副作用 | CASE `low price rejection does not append or persist`：HTTP `400`，code `40003`；stream count `1/1`；BidLog count `1/1`；Redis state 仍为 `current_price=1100`、leader 不变 | PASS |
| 多用户排名和最终一致 | CASE `second bid ranking and async log consistency`：第二次出价 HTTP `200`，`bid_id=bid_2062488166807375872`；ranking top 为 `user_2062488164869607424/1200`；MySQL `bid_logs` 2 条；Redis stream 有两条本批次事件；dead stream 无本批次事件 | PASS |
| 清理 | CLEANUP：`DELETE FROM bid_logs WHERE item_id=?` rows=2；`DELETE FROM auction_rules WHERE item_id=?` rows=1；`UPDATE auction_items SET deleted_at=NOW()` rows=1；`UPDATE users SET deleted_at=NOW() WHERE account LIKE batch%` rows=3；Redis item keys removed | PASS |

## 通过项

- 合法出价接口返回成功，HTTP 响应、Redis state、Redis ranking、Redis stream handoff 和 worker 后 MySQL BidLog 最终一致。
- 同一 `idempotency_key` 重试不重复追加 stream、不重复写 BidLog、不重复增加 Redis 计数。
- 低价拒绝返回 `40003 price too low`，且不追加 stream、不新增 BidLog、不改变当前价或领先用户。
- 多用户连续出价后，当前价、领先用户、Redis ranking 第一名、HTTP ranking 第一名和 MySQL BidLog 一致。
- 本次测试只操作批次 `agent_bid_hotpath_20260604183926` 创建的数据，并记录了清理结果。

## 失败项

- 无。

## 跳过项

- Apifox 对齐：本次为模块集成测试，不是接口契约专项；后续接口契约测试应补充路径、字段、错误码对齐。
- 并发一致性：未执行；需要按 `guides/concurrency.md` 单独设计并等待批准。
- 一口价成交：未执行；本次聚焦普通 bid hot path 和 async BidLog。
- 自动延时：未执行；可作为后续状态一致性补测。
- worker 故障注入：未模拟 `CreateBidLogs` 或 `XACK` 失败；当前只验证成功消费路径。
- WebSocket：未连接真实 WS；完整消息契约转 `modules/ws.md`。

## Apifox 对齐偏差

- 未执行接口契约专项对齐。

## 风险和建议

- 建议补充 worker 故障注入测试，验证 DB 写失败或 ACK 失败时消息保持 pending 并可重试。
- 建议补充并发一致性测试，验证高并发下 stream 事件数量、BidLog 最终数量和 Redis ranking 是否一致。
- 建议补充一口价成交集成测试，确认 Redis stream、BidLog worker 和 MySQL `auction_items` ended 状态一致。

## 建议沉淀的回归测试

- 模块集成 runner：成功出价后等待 worker 落库，并断言 HTTP / Redis state / Redis stream / MySQL `bid_logs` 一致。
- 模块集成 runner：同一 `idempotency_key` 重试后 stream 和 BidLog 数量不变。
- 模块集成 runner：低价拒绝后 Redis state、stream 和 BidLog 不变。
- 故障注入测试：worker 持久化失败或 ACK 失败时不误记成功，并保留待重试消息。

## 已知缺口

- 未验证真实 WebSocket 事件。
- 未验证一口价、自动延时、保证金和并发竞争。
- 未验证 Prometheus/OTel 指标是否被外部 collector 成功采集。

## 测试数据清理结果

- 测试批次 ID：`agent_bid_hotpath_20260604183926`
- 创建的数据：3 个测试用户、1 个测试拍品、1 条 auction rule、2 条 bid log、本批次 item 相关 Redis state/ranking/names/idempotency/room state。
- 清理方式：按明确 item ID 删除 `bid_logs` 和 `auction_rules`；按明确 item ID 软删除 `auction_items`；按 account 前缀软删除 `users`；删除本批次 item Redis keys；从共享 ZSET 移除本批次 item。
- 清理结果：runner CLEANUP 全部返回成功，SQL rows 分别为 bid_logs=2、auction_rules=1、auction_items=1、users=3。
- 未清理原因：Redis stream 不做全局删除；本次只记录并验证本批次 stream 事件和 dead stream 结果，避免影响非本批次消息。
