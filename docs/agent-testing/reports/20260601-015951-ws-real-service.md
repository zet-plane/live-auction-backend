# 测试报告：WebSocket real service

## 基本信息

- 测试目标：按 `docs/agent-testing/modules/ws.md` 执行真实本地服务 WebSocket 模块测试。
- 测试类型：接口契约测试 / 模块集成测试 / WebSocket 场景测试 / 状态一致性测试。
- 测试时间：2026-06-01 01:59:51 Asia/Shanghai。
- 执行 agent：Codex 主 agent。
- 主 agent：Codex。
- 子 agent：未使用。
- 子 agent 结果摘要：未使用。
- 主 agent 复核结论：未使用。
- 冲突和处理：首次在沙箱内启动服务被本地网络权限阻止，按环境指南在沙箱外启动后端服务后继续。首次 runner 因测试代码在读超时后重复读取 gorilla websocket 触发 panic；已修正 runner、清理半执行批次，再用新批次重跑。
- Subagent cleanup：未使用。
- 并行数据隔离证明：不适用。
- 读取文档：`docs/agent-testing/README.md`、`docs/agent-testing/templates/protocol.md`、`docs/agent-testing/guides/runner.md`、`docs/agent-testing/guides/environment.md`、`docs/agent-testing/guides/go-runner.md`、`docs/agent-testing/modules/ws.md`、`docs/agent-testing/reports/README.md`。

## 测试环境

- 服务地址：本地后端服务，地址已省略，测试后已停止。
- 配置来源：`config.yaml` 本地配置。
- MySQL：本地 Docker MySQL，地址和凭据已省略。
- Redis：本地 Docker Redis，地址已省略。
- Apifox：未执行对齐。
- WebSocket：真实 gorilla WebSocket 客户端连接本地服务。

## 依赖策略

| 依赖 | 使用方式 | 原因 |
| --- | --- | --- |
| MySQL | 本地真实依赖 | 验证真实用户、房间、商品数据写入和清理 |
| Redis | 本地真实依赖 | 验证 ticket、online state、room/item state |
| WebSocket | 真实客户端连接本地服务 | 验证握手、事件、control ping、断线重连 |
| 外部服务 | 未使用 | 本模块测试不需要第三方服务 |

## 测试数据

- 测试批次 ID：`agent_ws_202606010156`。
- 创建数据：2 个测试用户、1 个直播间、1 个拍品、1 次出价、对应 Redis ticket / room / item keys。
- 复用数据：复用本地 Docker MySQL / Redis 空间，不复用业务实体。

## 执行步骤

1. 确认 Docker MySQL / Redis healthy。
2. 启动本地后端服务。
3. 运行真实服务 WebSocket runner。
4. Runner 创建本批次测试用户，设置商家身份。
5. Runner 创建并开播直播间。
6. Runner 签发 ticket，验证 TTL。
7. Runner 验证缺 ticket、坏 ticket、同 ticket 二次握手失败。
8. Runner 建立真实 WebSocket，验证 ticket 被消费、Redis online state 收敛。
9. Runner 发送客户端 `ping`，验证收到 `pong`。
10. Runner 创建、上架并开始拍品，验证 `auction_started`、`time_sync`、出价和排行榜。
11. Runner 等待服务端 WebSocket control `ping`。
12. Runner 断开后通过房间详情、商品详情、排行榜恢复现场，再重连验证 `auction_snapshot` 和 `time_sync`。
13. Runner 发送 `leave_room`，验证 Redis online state 收敛为 0。
14. Runner 清理本批次 Redis keys 和 DB rows。
15. 停止本地后端服务，并验证 8080 端口不再监听。

## 验证证据

| 验证点 | 证据 | 结果 |
| --- | --- | --- |
| 服务健康 | Runner `service health`：HTTP status=200 | 通过 |
| 用户和商家身份准备 | Runner `register users`：注册两个用户、商家身份更新均 status=200 | 通过 |
| 房间创建和开播 | Runner `create and start room`：create=200、start=200、detail=200、status=live | 通过 |
| ticket TTL | Runner `ticket ttl`：ticket_len=32、TTL=45s | 通过 |
| 握手失败路径和 ticket 一次性消费 | Runner `handshake failures and ticket one-shot`：缺 ticket=400、坏 ticket=401、首次握手=101、二次握手=401 | 通过 |
| 有效握手和在线状态 | Runner `valid handshake and online state`：ticket=200、ws=101、ticket_gone=true、Redis online_count=1、SISMEMBER=true | 通过 |
| 客户端 ping/pong | Runner `client ping pong`：写入 `{"type":"ping"}` 后收到 `pong` | 通过 |
| 业务事件和排行榜 | Runner `item events and ranking`：create/publish/start 均 200，收到 `auction_started` 和 `time_sync`，bid=200，ranking=200，Redis item state=ongoing | 通过 |
| 服务端主动 heartbeat | Runner `server control ping`：control_ping_received=true | 通过 |
| 断线重连恢复 | Runner `reconnect recovery`：HTTP room/item/ranking 均 200，current_item 匹配，重连 101，收到 `auction_snapshot` 和 `time_sync` | 通过 |
| leave_room 在线数收敛 | Runner `leave room convergence`：Redis online_count=0，SISMEMBER=false | 通过 |
| 汇总 | Runner SUMMARY：PASS=11，FAIL=0 | 通过 |
| 清理 | Runner CLEANUP：删除 Redis room/item keys；DB soft cleanup auction_items=1、live_rooms=1、users=2 | 通过 |
| 半执行批次清理 | 旧批次 Redis 删除 5 个 key；DB 前缀清理后无 active rows | 通过 |
| 服务停止 | 停止临时后端服务后，8080 无监听进程 | 通过 |

## 通过项

- `POST /api/v1/ws-ticket` 真实签发、ticket TTL、一次性消费通过。
- WebSocket 握手 400 / 401 / 101 路径通过。
- 客户端文本 `ping -> pong` 通过。
- 服务端 WebSocket control `ping` 通过。
- Redis `online_count = SCARD online_users` 的真实收敛证据通过。
- `leave_room` 后 Redis 在线状态收敛为 0。
- 真实业务事件 `auction_started`、`time_sync` 可被客户端接收。
- 断线后通过 HTTP 房间详情、商品详情、排行榜恢复现场，再重连收到 `auction_snapshot` 和 `time_sync`。
- 本批次测试数据已清理，临时后端服务已停止。

## 失败项

- 无。

## 跳过项

- 并发握手 / 并发 online state：本轮不是并发一致性测试；按协议需要先输出并发场景设计并等待批准。
- 性能压测：本轮不是性能测试。
- Apifox 对齐：本轮聚焦真实服务功能验证，未执行 Apifox 对齐。
- 日志敏感信息全量检索：本轮未做日志系统检索，只确保报告不包含 token、完整 ticket、完整 query string 或 DSN。

## Apifox 对齐偏差

- 未执行。

## 风险和建议

- 建议后续补并发测试：同一 ticket 并发握手只能 1 个 101，其余 401。
- 建议后续补在线状态并发增减：多用户连接/断开并发后，Redis `online_count` 必须等于 `SCARD online_users`。
- 建议将本 runner 的真实 Redis / WebSocket 场景沉淀为可复用 Go runner，避免后续手写测试漂移。

## 建议沉淀的回归测试

- 真实 WebSocket runner：ticket TTL、一次性消费、握手失败路径、ping/pong、control ping、leave_room、online state。
- 真实重连恢复 runner：HTTP room/item/ranking 恢复 + `auction_snapshot` / `time_sync` 重连验证。
- 并发 runner：同 ticket 并发握手和 Redis online state 并发收敛。

## 已知缺口

- 未执行并发一致性测试。
- 未执行性能压测。
- 未执行 Apifox 对齐。
- 未做日志系统敏感信息检索。

## 测试数据清理结果

- 测试批次 ID：`agent_ws_202606010156`。
- 创建的数据：2 个测试用户、1 个直播间、1 个拍品、1 次出价、对应 Redis room/item/ticket 数据。
- 清理方式：Runner 删除本批次 Redis keys，按批次前缀软删除 DB 中 users、live_rooms、auction_items。
- 清理结果：Redis 相关 key 删除成功；DB cleanup rows：auction_items=1、live_rooms=1、users=2；复核查询 active rows 均为 0。
- 未清理原因：不适用。
